## Как запустить проект

1. `python manage.py runserver` (обязательно иметь настроенный .env.local, шаблон находится в .env.example)
2. `docker compose --env-file .env.docker up --build` (обязательно иметь настроенный .env.docker, шаблон находится в .env.example)
